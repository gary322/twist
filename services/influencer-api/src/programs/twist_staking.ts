// Placeholder Anchor IDL for the Twist staking program.
//
// The repo does not currently include a generated IDL JSON. For production deployments
// you should replace this with the real IDL from your Anchor build (`target/idl/*.json`)
// and keep it in sync with the on-chain program.
export const IDL: any = {
  version: '0.0.0',
  name: 'twist_staking',
  instructions: [],
  accounts: [],
  types: [],
};

